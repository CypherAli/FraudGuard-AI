<?xml version="1.0" encoding="UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FraudGuardAI.App">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Core Brand Colors -->
            <Color x:Key="BackgroundDark">#0D1B2A</Color>
            <Color x:Key="CardBackground">#1B2838</Color>
            <Color x:Key="InputBackground">#0F1923</Color>
            <Color x:Key="BorderColor">#2A3F54</Color>
            
            <!-- Text Colors -->
            <Color x:Key="TextPrimary">#E0E6ED</Color>
            <Color x:Key="TextSecondary">#8B9CAF</Color>
            
            <!-- Status Colors -->
            <Color x:Key="SafeColor">#34D399</Color>
            <Color x:Key="DangerColor">#F87171</Color>
            <Color x:Key="WarningColor">#FBBF24</Color>
            <Color x:Key="PrimaryBlue">#60A5FA</Color>
            <Color x:Key="InactiveColor">#5C6B7A</Color>
            
            <!-- Background States -->
            <Color x:Key="DangerBackground">#7F1D1D</Color>
            <Color x:Key="WarningBackground">#78350F</Color>
            
            <!-- Transparent Colors -->
            <Color x:Key="GlowGreen">#34D39960</Color>
            <Color x:Key="GlowRed">#F8717160</Color>
            <Color x:Key="AccentBlue20">#60A5FA20</Color>
            <Color x:Key="AccentYellow20">#FBBF2420</Color>
            
            <!-- Additional Colors (Aliases) -->
            <Color x:Key="CardBg">#1B2838</Color>
            <Color x:Key="InputBg">#0F1923</Color>
            <Color x:Key="SafeGreen">#34D399</Color>
            <Color x:Key="DangerRed">#F87171</Color>
            <Color x:Key="AccentBlue">#60A5FA</Color>
            <Color x:Key="WarningYellow">#FBBF24</Color>

            <!-- Global Styles -->
            <Style TargetType="ContentPage" ApplyToDerivedTypes="True">
                <Setter Property="BackgroundColor" Value="{StaticResource BackgroundDark}" />
            </Style>

            <Style TargetType="Label" ApplyToDerivedTypes="True">
                <Setter Property="TextColor" Value="{StaticResource TextPrimary}" />
            </Style>
            
            <!-- Card Style -->
            <Style x:Key="CardBorder" TargetType="Border">
                <Setter Property="BackgroundColor" Value="{StaticResource CardBackground}" />
                <Setter Property="StrokeThickness" Value="0" />
                <Setter Property="Padding" Value="20" />
                <Setter Property="StrokeShape">
                    <RoundRectangle CornerRadius="20"/>
                </Setter>
                <Setter Property="Shadow">
                    <Shadow Brush="#000000" Opacity="0.25" Radius="16" Offset="0,6"/>
                </Setter>
            </Style>
            
            <!-- Input Style -->
            <Style x:Key="InputBorder" TargetType="Border">
                <Setter Property="BackgroundColor" Value="{StaticResource InputBackground}" />
                <Setter Property="StrokeThickness" Value="1" />
                <Setter Property="Stroke" Value="{StaticResource BorderColor}" />
                <Setter Property="Padding" Value="4" />
                <Setter Property="StrokeShape">
                    <RoundRectangle CornerRadius="12"/>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
