<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FraudGuardAI.MainPage"
             Shell.NavBarIsVisible="False">

    <ScrollView>
        <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,*" Padding="20,48,20,20">
        
            <!-- Header -->
            <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" Margin="0,0,0,8">
                <HorizontalStackLayout Spacing="8" VerticalOptions="Center">
                    <Border WidthRequest="32" HeightRequest="32" 
                            BackgroundColor="{StaticResource CyanAccent}"
                            StrokeThickness="0">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="8"/>
                        </Border.StrokeShape>
                        <Label Text="🛡" FontSize="16" 
                               HorizontalOptions="Center" 
                               VerticalOptions="Center"/>
                    </Border>
                    <Label Text="CallGuard" 
                           FontSize="20" 
                           FontAttributes="Bold"
                           TextColor="{StaticResource TextPrimary}"
                           VerticalOptions="Center"/>
                </HorizontalStackLayout>
                
                <!-- Progress Bar -->
                <Border Grid.Column="1" 
                        HeightRequest="4" 
                        BackgroundColor="{StaticResource ProgressBarBg}"
                        Margin="16,0"
                        VerticalOptions="Center">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="2"/>
                    </Border.StrokeShape>
                </Border>
                
                <Label Grid.Column="2" Text="☰" FontSize="24" 
                       TextColor="{StaticResource TextSecondary}"
                       VerticalOptions="Center"/>
            </Grid>
            
            <!-- Title Section -->
            <VerticalStackLayout Grid.Row="1" Spacing="4" Margin="0,16,0,16">
                <Label Text="Bảng điều khiển"
                       FontSize="28"
                       FontAttributes="Bold"
                       TextColor="{StaticResource TextPrimary}"/>
                
                <Label Text="Theo dõi và quản lý các cuộc gọi lừa đảo"
                       FontSize="14"
                       TextColor="{StaticResource TextSecondary}"/>
                
                <!-- Report Button -->
                <Button x:Name="ReportButton"
                        Text="+ Báo cáo số mới"
                        FontSize="14"
                        FontAttributes="Bold"
                        BackgroundColor="{StaticResource OrangeIcon}"
                        TextColor="White"
                        CornerRadius="8"
                        HeightRequest="40"
                        HorizontalOptions="Start"
                        Padding="16,0"
                        Margin="0,12,0,0"
                        Clicked="OnReportButtonClicked"/>
            </VerticalStackLayout>
            
            <!-- Protection Status Card -->
            <Border Grid.Row="2" 
                    BackgroundColor="{StaticResource CardBg}"
                    StrokeThickness="0"
                    Padding="24"
                    Margin="0,8,0,16">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="20"/>
                </Border.StrokeShape>
                <Border.Shadow>
                    <Shadow Brush="#000000" Opacity="0.3" Radius="20" Offset="0,8"/>
                </Border.Shadow>
                
                <VerticalStackLayout Spacing="12" HorizontalOptions="Center">
                    <!-- Shield Icon with Glow -->
                    <Border x:Name="ShieldBorder"
                            WidthRequest="80"
                            HeightRequest="80"
                            BackgroundColor="#1E3A4D"
                            StrokeThickness="2"
                            Stroke="{StaticResource TealIcon}"
                            HorizontalOptions="Center">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="40"/>
                        </Border.StrokeShape>
                        <Border.Shadow>
                            <Shadow Brush="{StaticResource TealIcon}" Opacity="0.4" Radius="20"/>
                        </Border.Shadow>
                        
                        <Grid>
                            <Label Text="✓" FontSize="32"
                                   TextColor="{StaticResource TealIcon}"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center"/>
                        </Grid>
                    </Border>
                    
                    <Label x:Name="ProtectionStatusLabel"
                           Text="Đang bảo vệ"
                           FontSize="12"
                           TextColor="{StaticResource TealIcon}"
                           HorizontalOptions="Center"/>
                    
                    <Label x:Name="StatusLabel"
                           Text="Bảo vệ đang hoạt động"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{StaticResource TextPrimary}"
                           HorizontalOptions="Center"/>
                    
                    <Label x:Name="BlockRateLabel"
                           Text="Chưa có dữ liệu"
                           FontSize="14"
                           TextColor="{StaticResource TextSecondary}"
                           HorizontalOptions="Center"/>
                    
                    <!-- Toggle Protection Button -->
                    <Button x:Name="ToggleProtectionButton"
                            Text="Kích hoạt bảo vệ"
                            FontSize="14"
                            FontAttributes="Bold"
                            BackgroundColor="{StaticResource CyanAccent}"
                            TextColor="White"
                            CornerRadius="20"
                            HeightRequest="44"
                            WidthRequest="180"
                            HorizontalOptions="Center"
                            Margin="0,8,0,0"
                            Clicked="OnToggleProtectionClicked"/>
                </VerticalStackLayout>
            </Border>
            
            <!-- Stats Cards Grid -->
            <Grid Grid.Row="3" 
                  ColumnDefinitions="*,*" 
                  RowDefinitions="Auto,Auto"
                  ColumnSpacing="12"
                  RowSpacing="12"
                  Margin="0,0,0,16">
                
                <!-- Card 1: Số đã chặn -->
                <Border Grid.Row="0" Grid.Column="0"
                        BackgroundColor="{StaticResource StatCardBg}"
                        StrokeThickness="0"
                        Padding="16">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="16"/>
                    </Border.StrokeShape>
                    
                    <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="8">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="SỐ ĐÃ CHẶN"
                                   FontSize="11"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource TextSecondary}"/>
                            <Border WidthRequest="28" HeightRequest="28"
                                    BackgroundColor="{StaticResource OrangeIcon}"
                                    StrokeThickness="0"
                                    HorizontalOptions="End">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8"/>
                                </Border.StrokeShape>
                                <Label Text="🚫" FontSize="14"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center"/>
                            </Border>
                        </HorizontalStackLayout>
                        
                        <Label Grid.Row="1" x:Name="BlockedTotalLabel"
                               Text="0"
                               FontSize="36"
                               FontAttributes="Bold"
                               TextColor="{StaticResource TextPrimary}"/>
                        
                        <Label Grid.Row="2" x:Name="WeeklyChangeLabel"
                               Text=""
                               IsVisible="False"
                               FontSize="12"
                               TextColor="{StaticResource TealIcon}"/>
                    </Grid>
                </Border>
                
                <!-- Card 2: Chặn hôm nay -->
                <Border Grid.Row="0" Grid.Column="1"
                        BackgroundColor="{StaticResource StatCardBg}"
                        StrokeThickness="0"
                        Padding="16">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="16"/>
                    </Border.StrokeShape>
                    
                    <Grid RowDefinitions="Auto,Auto" RowSpacing="8">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="CHẶN HÔM NAY"
                                   FontSize="11"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource TextSecondary}"/>
                            <Border WidthRequest="28" HeightRequest="28"
                                    BackgroundColor="{StaticResource BlueIcon}"
                                    StrokeThickness="0"
                                    HorizontalOptions="End">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8"/>
                                </Border.StrokeShape>
                                <Label Text="📞" FontSize="14"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center"/>
                            </Border>
                        </HorizontalStackLayout>
                        
                        <Label Grid.Row="1" x:Name="BlockedTodayLabel"
                               Text="0"
                               FontSize="36"
                               FontAttributes="Bold"
                               TextColor="{StaticResource TextPrimary}"/>
                    </Grid>
                </Border>
                
                <!-- Card 3: Mối đe dọa nghiêm trọng -->
                <Border Grid.Row="1" Grid.Column="0"
                        BackgroundColor="{StaticResource StatCardBg}"
                        StrokeThickness="0"
                        Padding="16">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="16"/>
                    </Border.StrokeShape>
                    
                    <Grid RowDefinitions="Auto,Auto" RowSpacing="8">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="MỐI ĐE DỌA NGHIÊM TRỌNG"
                                   FontSize="10"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource TextSecondary}"
                                   LineBreakMode="TailTruncation"/>
                            <Border WidthRequest="28" HeightRequest="28"
                                    BackgroundColor="{StaticResource RedIcon}"
                                    StrokeThickness="0"
                                    HorizontalOptions="End">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8"/>
                                </Border.StrokeShape>
                                <Label Text="⚠" FontSize="14"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center"/>
                            </Border>
                        </HorizontalStackLayout>
                        
                        <Label Grid.Row="1" x:Name="ThreatsLabel"
                               Text="0"
                               FontSize="36"
                               FontAttributes="Bold"
                               TextColor="{StaticResource TextPrimary}"/>
                    </Grid>
                </Border>
                
                <!-- Card 4: Hiệu quả bảo vệ -->
                <Border Grid.Row="1" Grid.Column="1"
                        BackgroundColor="{StaticResource StatCardBg}"
                        StrokeThickness="0"
                        Padding="16">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="16"/>
                    </Border.StrokeShape>
                    
                    <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="8">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="HIỆU QUẢ BẢO VỆ"
                                   FontSize="11"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource TextSecondary}"/>
                            <Border WidthRequest="28" HeightRequest="28"
                                    BackgroundColor="{StaticResource BlueIcon}"
                                    StrokeThickness="0"
                                    HorizontalOptions="End">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="8"/>
                                </Border.StrokeShape>
                                <Label Text="📈" FontSize="14"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center"/>
                            </Border>
                        </HorizontalStackLayout>
                        
                        <Label Grid.Row="1" x:Name="EfficiencyLabel"
                               Text="0%"
                               FontSize="32"
                               FontAttributes="Bold"
                               TextColor="{StaticResource TextPrimary}"/>
                        
                        <Label Grid.Row="2" x:Name="EfficiencyChangeLabel"
                               Text=""
                               IsVisible="False"
                               FontSize="12"
                               TextColor="{StaticResource TealIcon}"/>
                    </Grid>
                </Border>
            </Grid>
            
            <!-- Recent Calls Section -->
            <Grid Grid.Row="4" ColumnDefinitions="*,Auto" Margin="0,8,0,12">
                <Label Text="Cuộc gọi gần đây"
                       FontSize="16"
                       FontAttributes="Bold"
                       TextColor="{StaticResource TextPrimary}"
                       VerticalOptions="Center"/>
                
                <HorizontalStackLayout Grid.Column="1" Spacing="4">
                    <Label Text="🕐" FontSize="12" VerticalOptions="Center"/>
                    <Label Text="Hôm nay"
                           FontSize="13"
                           TextColor="{StaticResource TextSecondary}"
                           VerticalOptions="Center"/>
                </HorizontalStackLayout>
            </Grid>
            
            <!-- Recent Calls List (Placeholder) -->
            <VerticalStackLayout Grid.Row="5" x:Name="RecentCallsContainer">
                <Border BackgroundColor="{StaticResource StatCardBg}"
                        StrokeThickness="0"
                        Padding="16">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="12"/>
                    </Border.StrokeShape>
                    
                    <VerticalStackLayout Spacing="8" HorizontalOptions="Center" VerticalOptions="Center">
                        <Label Text="📱" FontSize="32" HorizontalOptions="Center"/>
                        <Label Text="Chưa có cuộc gọi nào"
                               FontSize="14"
                               TextColor="{StaticResource TextSecondary}"
                               HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>
            
            <!-- Alert Banner (Hidden by default) -->
            <Border x:Name="AlertBanner"
                    Grid.Row="5"
                    IsVisible="False"
                    BackgroundColor="{StaticResource DangerBackground}"
                    Padding="20,16"
                    Margin="0,12,0,0"
                    StrokeThickness="1"
                    Stroke="{StaticResource GlowRed}">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="16"/>
                </Border.StrokeShape>
                
                <Grid ColumnDefinitions="Auto,*">
                    <Border WidthRequest="44"
                           HeightRequest="44"
                           BackgroundColor="#F8717130"
                           StrokeThickness="0"
                           VerticalOptions="Start">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="22"/>
                        </Border.StrokeShape>
                        <Label Text="⚠"
                               FontSize="22"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"/>
                    </Border>
                    
                    <VerticalStackLayout Grid.Column="1" Spacing="4" Margin="14,0,0,0">
                        <Label x:Name="AlertTypeLabel"
                               Text=""
                               FontSize="15"
                               FontAttributes="Bold"
                               TextColor="#FCA5A5"/>
                        <Label x:Name="AlertMessageLabel"
                               Text=""
                               FontSize="13"
                               TextColor="#FECACA"
                               LineBreakMode="TailTruncation"/>
                        <Label x:Name="AlertConfidenceLabel"
                               Text=""
                               FontSize="12"
                               TextColor="#FCA5A5"
                               Opacity="0.8"/>
                    </VerticalStackLayout>
                </Grid>
            </Border>
        </Grid>
    </ScrollView>
</ContentPage>
