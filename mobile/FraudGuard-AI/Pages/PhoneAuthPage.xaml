<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FraudGuardAI.Pages.PhoneAuthPage"
             xmlns:loc="clr-namespace:FraudGuardAI.Localization"
             Title="{loc:Translate Key=PhoneAuth_Title}"
             BackgroundColor="#F5F5F5">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="20">
            
            <!-- Logo/Header -->
            <Frame BackgroundColor="White" 
                   CornerRadius="16" 
                   Padding="24" 
                   HasShadow="True"
                   Margin="0,20,0,0">
                <VerticalStackLayout Spacing="12">
                    <Label Text="🔐" 
                           FontSize="60" 
                           HorizontalOptions="Center"/>
                    <Label Text="{loc:Translate Key=PhoneAuth_HeaderTitle}" 
                           FontSize="28" 
                           FontAttributes="Bold" 
                           HorizontalTextAlignment="Center"
                           TextColor="#2196F3"/>
                    <Label FontSize="14" 
                           HorizontalTextAlignment="Center"
                           TextColor="Gray"
                           Text="{loc:Translate Key=PhoneAuth_HeaderSubtitle}"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Phone Number Input Section -->
            <Frame x:Name="PhoneInputFrame" 
                   BackgroundColor="White" 
                   CornerRadius="16" 
                   Padding="20" 
                   HasShadow="True"
                   IsVisible="True">
                <VerticalStackLayout Spacing="16">
                    <Label FontSize="18" 
                           FontAttributes="Bold"
                           TextColor="#333333"
                           Text="{loc:Translate Key=PhoneAuth_EnterPhoneTitle}"/>
                    
                    <Label FontSize="13"
                           TextColor="Gray"
                           Text="{loc:Translate Key=PhoneAuth_EnterPhoneDesc}"/>

                    <!-- Phone Number Entry -->
                    <Frame BorderColor="#E0E0E0" 
                           CornerRadius="8" 
                           Padding="0" 
                           HasShadow="False">
                        <Entry x:Name="PhoneNumberEntry"
                               Placeholder="+84 xxx xxx xxx"
                               Keyboard="Telephone"
                               FontSize="16"
                               TextColor="#333333"
                               PlaceholderColor="Gray"
                               MaxLength="15"/>
                    </Frame>

                    <!-- Send OTP Button -->
                  <Button x:Name="SendOtpButton"
                         Text="{loc:Translate Key=PhoneAuth_SendOtp}"
                            BackgroundColor="#2196F3"
                            TextColor="White"
                            FontSize="16"
                            FontAttributes="Bold"
                            CornerRadius="8"
                            HeightRequest="50"
                            Clicked="OnSendOtpClicked"/>

                    <!-- Loading Indicator -->
                    <ActivityIndicator x:Name="SendingIndicator"
                                     IsRunning="False"
                                     IsVisible="False"
                                     Color="#2196F3"
                                     HeightRequest="40"/>
                </VerticalStackLayout>
            </Frame>

            <!-- OTP Verification Section -->
            <Frame x:Name="OtpVerificationFrame" 
                   BackgroundColor="White" 
                   CornerRadius="16" 
                   Padding="20" 
                   HasShadow="True"
                   IsVisible="False">
                <VerticalStackLayout Spacing="16">
                    <Label FontSize="18" 
                           FontAttributes="Bold"
                           TextColor="#333333"
                           Text="{loc:Translate Key=PhoneAuth_EnterOtpTitle}"/>
                    
                    <Label x:Name="OtpSentLabel"
                           Text="{loc:Translate Key=PhoneAuth_OtpSent}"
                           FontSize="13"
                           TextColor="Gray"/>

                    <!-- OTP Code Entry -->
                    <Frame BorderColor="#E0E0E0" 
                           CornerRadius="8" 
                           Padding="0" 
                           HasShadow="False">
                        <Entry x:Name="OtpCodeEntry"
                               Placeholder="Nhập 6 chữ số"
                               Keyboard="Numeric"
                               FontSize="24"
                               TextColor="#333333"
                               PlaceholderColor="Gray"
                               MaxLength="6"
                               HorizontalTextAlignment="Center"
                               FontAttributes="Bold"/>
                    </Frame>

                    <!-- Verify OTP Button -->
                  <Button x:Name="VerifyOtpButton"
                         Text="{loc:Translate Key=PhoneAuth_Verify}"
                            BackgroundColor="#4CAF50"
                            TextColor="White"
                            FontSize="16"
                            FontAttributes="Bold"
                            CornerRadius="8"
                            HeightRequest="50"
                            Clicked="OnVerifyOtpClicked"/>

                    <!-- Verifying Indicator -->
                    <ActivityIndicator x:Name="VerifyingIndicator"
                                     IsRunning="False"
                                     IsVisible="False"
                                     Color="#4CAF50"
                                     HeightRequest="40"/>

                    <!-- Resend OTP -->
                    <HorizontalStackLayout Spacing="8" 
                                         HorizontalOptions="Center"
                                         Margin="0,8,0,0">
                        <Label FontSize="13"
                               TextColor="Gray"
                               VerticalOptions="Center"
                               Text="{loc:Translate Key=PhoneAuth_ResendQuestion}"/>
                        <Button x:Name="ResendOtpButton"
                                Text="{loc:Translate Key=PhoneAuth_Resend}"
                                BackgroundColor="Transparent"
                                TextColor="#2196F3"
                                FontSize="13"
                                FontAttributes="Bold"
                                Padding="0"
                                Clicked="OnResendOtpClicked"/>
                    </HorizontalStackLayout>

                    <!-- Change Phone Number -->
                  <Button Text="{loc:Translate Key=PhoneAuth_ChangePhone}"
                            BackgroundColor="Transparent"
                            TextColor="Gray"
                            FontSize="13"
                            Clicked="OnChangePhoneClicked"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Info Section -->
            <Frame BackgroundColor="#FFF3E0" 
                   CornerRadius="12" 
                   Padding="16" 
                   HasShadow="False"
                   BorderColor="#FFB300">
                <VerticalStackLayout Spacing="8">
                                   <Label FontSize="14" 
                                             FontAttributes="Bold"
                                             TextColor="#F57C00"
                                             Text="{loc:Translate Key=PhoneAuth_InfoTitle}"/>
                    <Label FontSize="12" TextColor="#E65100">
                        <Label.FormattedText>
                            <FormattedString>
                                                        <Span Text="{loc:Translate Key=PhoneAuth_InfoBullet1}"/>
                                                        <Span Text="&#x0a;"/>
                                                        <Span Text="{loc:Translate Key=PhoneAuth_InfoBullet2}"/>
                                                        <Span Text="&#x0a;"/>
                                                        <Span Text="{loc:Translate Key=PhoneAuth_InfoBullet3}"/>
                                                        <Span Text="&#x0a;"/>
                                                        <Span Text="{loc:Translate Key=PhoneAuth_InfoBullet4}"/>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                </VerticalStackLayout>
            </Frame>

            <!-- Privacy Notice -->
            <Label FontSize="11" 
                   TextColor="Gray" 
                   HorizontalTextAlignment="Center"
                   Margin="0,0,0,20">
                <Label.FormattedText>
                    <FormattedString>
                            <Span Text="{loc:Translate Key=PhoneAuth_PrivacyPrefix}"/>
                            <Span Text="{loc:Translate Key=PhoneAuth_Terms}" 
                                   TextColor="#2196F3" 
                                   TextDecorations="Underline"/>
                            <Span Text="{loc:Translate Key=PhoneAuth_And}"/>
                            <Span Text="{loc:Translate Key=PhoneAuth_Privacy}" 
                                   TextColor="#2196F3" 
                                   TextDecorations="Underline"/>
                            <Span Text="{loc:Translate Key=PhoneAuth_PrivacySuffix}"/>
                    </FormattedString>
                </Label.FormattedText>
            </Label>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>

